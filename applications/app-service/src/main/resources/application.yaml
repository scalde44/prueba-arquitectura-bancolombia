server:
  port: 8080
spring:
  application:
    name: "pruebaca"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      uri: "mongodb+srv://scalde44:Scalderon4@clusterprueba.bzwwx.mongodb.net/?retryWrites=true&w=majority"
      database: reactiveprueba
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
